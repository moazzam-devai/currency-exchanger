import { BaseConfig } from "./base-config";

export const ApiConfig = {
    latest: BaseConfig.baseApiUrl + 'latest',
    convert: BaseConfig.baseApiUrl + 'convert',
    symbols: BaseConfig.baseApiUrl + 'symbols',
    historical: BaseConfig.baseApiUrl
  };
  